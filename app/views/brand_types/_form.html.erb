<%= form_with(model: brand_type) do |form| %>
  <% if brand_type.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(brand_type.errors.count, "error") %> prohibited this brand_type from being saved:</h2>

      <ul>
        <% brand_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :brand_name, style: "display: block" %>
    <%= form.text_field :brand_name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
