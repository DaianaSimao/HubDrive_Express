<%= form_with(model: rentaltype) do |form| %>
  <% if rentaltype.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(rentaltype.errors.count, "error") %> prohibited this rentaltype from being saved:</h2>

      <ul>
        <% rentaltype.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :rental_type, style: "display: block" %>
    <%= form.text_field :rental_type %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
