<p style="color: green"><%= notice %></p>

<h1>Address types</h1>

<div id="address_types">
  <% @address_types.each do |address_type| %>
    <%= render address_type %>
    <p>
      <%= link_to "Show this address type", address_type %>
    </p>
  <% end %>
</div>

<%= link_to "New address type", new_address_type_path %>
