<%= form_with(model: type_address) do |form| %>
  <% if type_address.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(type_address.errors.count, "error") %> prohibited this type_address from being saved:</h2>

      <ul>
        <% type_address.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :type_address, style: "display: block" %>
    <%= form.text_field :type_address %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
